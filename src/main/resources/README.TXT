README:

Double-Click "InstallCerts.bat" to install the downloaded certificates to the Java KeyStores you selected.

TROUBLESHOOTING:

You may see an "Access is Denied" message when attempting to install the certificates.
Rerun the installer as an administrator so that you have the required permission.